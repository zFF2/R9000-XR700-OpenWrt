--- a/files/etc/init.d/geomate
+++ b/files/etc/init.d/geomate
@@ -4,17 +4,15 @@
 
 USE_PROCD=1
 
-VERSION="dev"
+VERSION="1.2.0"
 UPD_CHANNEL="release"
 
 START=95
 STOP=01
 
-EXTRA_COMMANDS="status geolocate check_version update health_check cleanup_ips"
+EXTRA_COMMANDS="status geolocate health_check cleanup_ips"
 EXTRA_HELP="        status          Get detailed service status
         geolocate       Run geolocation for IP addresses
-        check_version   Check for updates
-        update          Update geomate
         health_check    Check if Geomate is properly configured
         cleanup_ips     Remove private/invalid IPs from all IP lists"
 
@@ -24,7 +22,7 @@
 DEFAULT_IFS=" 	${_NL_}"
 IFS="$DEFAULT_IFS"
 
-cmd="/etc/geomate.sh"
+cmd="/usr/libexec/geomate/geomate.sh"
 name="geomate"
 pid_file="/var/run/${name}.pid"
 
@@ -63,14 +61,14 @@
 GEOMATE_FILE_TYPES_BACKEND="GEN EXTRA" # change the value if adding more types
 GEOMATE_GEN_FILES_BACKEND="
 	$GEOMATE_SERVICE_PATH
-	/etc/geomate.sh
-	/etc/geomate_trigger.sh
-	/etc/geolocate.sh"
+	/usr/libexec/geomate/geomate.sh
+	/usr/libexec/geomate/geomate_trigger.sh
+	/usr/libexec/geomate/geolocate.sh"
 GEOMATE_EXEC_FILES_BACKEND="
 	$GEOMATE_SERVICE_PATH
-	/etc/geomate.sh
-	/etc/geomate_trigger.sh
-	/etc/geolocate.sh"
+	/usr/libexec/geomate/geomate.sh
+	/usr/libexec/geomate/geomate_trigger.sh
+	/usr/libexec/geomate/geolocate.sh"
 GEOMATE_EXTRA_FILES_BACKEND="" # might be useful in the future?
 
 ### Frontend files
@@ -185,48 +183,22 @@
 }
 
 start_service() {
-	# handle first run after installation or update from older versions
-	if [ "$VERSION" = dev ]; then
-		log_msg "" "Completing the upgrade of the update mechanism..."
-		update -f || return 1
-		# shellcheck disable=SC1090
-		. "${GEOMATE_SERVICE_PATH}" # source updated init script 
-	fi
-
 	for component in $GEOMATE_COMPONENTS; do
 		check_files_integrity "$component"
 		case $? in
-			0) ;; # integrity OK
+			0|3) ;; # integrity OK / checksum drift tolerated in package mode
 			1) return 1 ;;
-			2) # missing files
-				local fix_upd_channel='' fix_version='' force_version=''
-				case "$component" in
-					BACKEND) fix_version="$VERSION" fix_upd_channel="$UPD_CHANNEL" ;;
-					FRONTEND) get_component_spec fix_version fix_upd_channel FRONTEND local 2>/dev/null
-				esac
-				case "$fix_version" in
-					dev|''|"1.0.0"|"v1.0.0") ;;
-					*) force_version="-W $fix_version"
-				esac
-				: "${fix_upd_channel:=release}"
-				update -U "$fix_upd_channel" $force_version -c "$component" || return 1 ;;
-			3) ;; # non-matching md5sums
+			2)
+				log_msg -warn "Missing files in component '$component'. Self-update is disabled in package-managed mode." ;;
 		esac
 	done
 
-    # Recovery: Download default config if missing (e.g., after failed update)
+    # Recovery: Recreate a minimal config if missing
     if [ ! -f /etc/config/geomate ]; then
-        log_msg -warn "Config file missing, downloading default configuration from repository"
-        local config_url="${GEOMATE_GH_RAW_URL_BACKEND_MAIN}/files/etc/config/geomate"
-        if uclient-fetch "$config_url" -O /etc/config/geomate 2>/dev/null; then
-            chmod 644 /etc/config/geomate
-            log_msg "Default configuration downloaded successfully"
-        else
-            log_msg -err "Failed to download config, creating minimal fallback"
-            echo "config global 'global'
+        log_msg -warn "Config file missing, creating minimal fallback configuration"
+        echo "config global 'global'
 	option enabled '1'" > /etc/config/geomate
-            chmod 644 /etc/config/geomate
-        fi
+        chmod 644 /etc/config/geomate
     fi
 
     local enabled
@@ -262,7 +234,7 @@
 }
 
 geolocate() {
-    /etc/geolocate.sh
+    /usr/libexec/geomate/geolocate.sh
 }
 
 status_service() {
@@ -539,7 +511,10 @@
 
     # Check 4: Required files exist
     local files_ok=1
-    for file in /etc/geomate.sh /etc/geolocate.sh /etc/geomate_trigger.sh; do
+    for file in \
+        /usr/libexec/geomate/geomate.sh \
+        /usr/libexec/geomate/geolocate.sh \
+        /usr/libexec/geomate/geomate_trigger.sh; do
         if [ ! -f "$file" ]; then
             status="${status}file_${file##*/}:missing;"
             errors=$((errors + 1))
@@ -1219,6 +1194,11 @@
 # -f : force update
 # -i : ignore previous cache results
 update() {
+	error_out "Built-in updater is disabled in this OpenWrt package build."
+	error_out "Use package upgrades instead (e.g. apk/opkg upgrade geomate luci-app-geomate)."
+	error_out "Run: /etc/init.d/geomate restart after package upgrades."
+	return 1
+
 	upd_failed() {
 		rm -rf "${GEOMATE_UPD_DIR:-?}"
 		[ -n "$*" ] && error_out "$@"
