--- a/files/etc/init.d/geomate
+++ b/files/etc/init.d/geomate
@@ -4,17 +4,15 @@
 
 USE_PROCD=1
 
-VERSION="dev"
+VERSION="1.2.0"
 UPD_CHANNEL="release"
 
 START=95
 STOP=01
 
-EXTRA_COMMANDS="status geolocate check_version update health_check cleanup_ips"
+EXTRA_COMMANDS="status geolocate health_check cleanup_ips"
 EXTRA_HELP="        status          Get detailed service status
         geolocate       Run geolocation for IP addresses
-        check_version   Check for updates
-        update          Update geomate
         health_check    Check if Geomate is properly configured
         cleanup_ips     Remove private/invalid IPs from all IP lists"
 
@@ -24,7 +22,7 @@
 DEFAULT_IFS=" 	${_NL_}"
 IFS="$DEFAULT_IFS"
 
-cmd="/etc/geomate.sh"
+cmd="/usr/libexec/geomate/geomate.sh"
 name="geomate"
 pid_file="/var/run/${name}.pid"
 
@@ -63,14 +61,14 @@
 GEOMATE_FILE_TYPES_BACKEND="GEN EXTRA" # change the value if adding more types
 GEOMATE_GEN_FILES_BACKEND="
 	$GEOMATE_SERVICE_PATH
-	/etc/geomate.sh
-	/etc/geomate_trigger.sh
-	/etc/geolocate.sh"
+	/usr/libexec/geomate/geomate.sh
+	/usr/libexec/geomate/geomate_trigger.sh
+	/usr/libexec/geomate/geolocate.sh"
 GEOMATE_EXEC_FILES_BACKEND="
 	$GEOMATE_SERVICE_PATH
-	/etc/geomate.sh
-	/etc/geomate_trigger.sh
-	/etc/geolocate.sh"
+	/usr/libexec/geomate/geomate.sh
+	/usr/libexec/geomate/geomate_trigger.sh
+	/usr/libexec/geomate/geolocate.sh"
 GEOMATE_EXTRA_FILES_BACKEND="" # might be useful in the future?
 
 ### Frontend files
@@ -344,13 +316,16 @@
 
     # Check nftables status
-    print_msg "" "==== nftables Status ===="
-    if nft list table inet geomate >/dev/null 2>&1; then
-        print_msg "Table inet geomate: active"
+    print_msg "" "==== Geomate nftables Table ===="
+    if ! command -v nft >/dev/null 2>&1; then
+        print_msg "nft command: missing"
+    elif nft list table inet geomate >/dev/null 2>&1; then
+        print_msg "Geomate table: active"
         local set_count
         set_count=$(nft list sets inet geomate 2>/dev/null | grep -c "set geomate_")
         print_msg "Sets: $set_count active"
     else
-        print_msg "Table inet geomate: NOT active"
+        print_msg "Geomate table: not active"
+        print_msg "nftables itself is installed; start Geomate to create its table."
     fi
 
     # Show geolocation status
@@ -366,7 +338,10 @@
 
     # Show version info
     print_msg "" "==== Version Information ===="
-    check_version -n
+    print_msg "Backend versions:"
+    print_msg "  Update channel: $UPD_CHANNEL"
+    print_msg "  Current version: $VERSION"
+    print_msg "  Latest version: package-managed"
 
     # Run health check
     print_msg "" "==== Health Check ===="
@@ -529,10 +505,13 @@
 
     # Check 3: nftables table exists
-    if nft list table inet geomate >/dev/null 2>&1; then
-        status="${status}nft:ok;"
+    if ! command -v nft >/dev/null 2>&1; then
+        status="${status}nft_cmd:missing;"
+        errors=$((errors + 1))
+    elif nft list table inet geomate >/dev/null 2>&1; then
+        status="${status}nft_table:ok;"
     else
-        status="${status}nft:missing;"
-        if [ "$global_enabled" = "1" ]; then
+        status="${status}nft_table:inactive;"
+        if [ "$global_enabled" = "1" ] && [ "$service_enabled" = "1" ]; then
             errors=$((errors + 1))
         fi
     fi
@@ -539,7 +511,10 @@
 
     # Check 4: Required files exist
     local files_ok=1
-    for file in /etc/geomate.sh /etc/geolocate.sh /etc/geomate_trigger.sh; do
+    for file in \
+        /usr/libexec/geomate/geomate.sh \
+        /usr/libexec/geomate/geolocate.sh \
+        /usr/libexec/geomate/geomate_trigger.sh; do
         if [ ! -f "$file" ]; then
             status="${status}file_${file##*/}:missing;"
             errors=$((errors + 1))

